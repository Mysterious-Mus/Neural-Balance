% Experimental results.

\section{Evaluation}
\label{sec:evaluation}

\subsection{Experimental setup}
This section should describe the datasets, models, optimizers, training budgets, and hyperparameter search spaces used for each method. When reporting results, include enough detail to reproduce the runs (e.g., seed list, learning rate, batch size, and regularization coefficients).

\subsection{Main results}
Report the main comparison table(s) here, typically including:
\begin{itemize}
  \item baseline (no explicit regularization),
  \item classical regularizers (weight decay, dropout, etc.),
  \item neural balancing (before training and/or interleaved),
  \item combinations (e.g., weight decay + balancing).
\end{itemize}

\subsection{Data-scarce regime}
Because regularization effects are often most visible when data is limited, include a controlled ablation where the training set is reduced (e.g., 1\%, 5\%, 10\% of the full training data) while keeping the test set unchanged.

\subsection{Ablations}
Suggested ablations:
\begin{itemize}
  \item balancing cost choice: $L_1$ vs.\ $L_2$,
  \item balancing schedule: before training vs.\ every $k$ epochs,
  \item model size: small vs.\ medium vs.\ large,
  \item interaction with weight decay and dropout.
\end{itemize}

